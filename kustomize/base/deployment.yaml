apiVersion: apps/v1
kind: Deployment
metadata:
  name: fint-kontroll-mock-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: fint-kontroll-resource-gateway
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - image: ghcr.io/fintlabs/fint-kontroll-resource-gateway:latest
          name: fint-kontroll-resource-gateway
          resources:
            limits:
              memory: "2048Mi"
              cpu: "1"
            requests:
              memory: "512Mi"
              cpu: "100m"
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: fint-kontroll-resource-gateway

